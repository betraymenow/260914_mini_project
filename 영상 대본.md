마지막 다섯 번째 섹션 미니 프로젝트 구현입니다. 지금까지 5일 동안 배운 내용을 하나의 작은 프로젝트로 정리해 보는 시간입니다.이 영상에서는 프로젝트의 목표, 완료 기준, 진행 방법, 시작 코드 사용 방법을 안내하고 실제 구현은 여러분이 직접 진행하게 됩니다. 먼저 미니 프로젝트의 목표입니다. 여러분이 직접 정한 도메인을 다루는 채봇 AP 를 만드는 것이 목표입니다. 5일차에서 배운 레이어드 구조 위에 3일차와 4일차의 LM 호출과 스트리밍 응답, 2일차의 파이덴팅 모델, 1일자의 HTTP 기본 개념을 모두 연결해 봅니다. 주제는 정해져 있지 않습니다. 요리 레시피 도움이, 영화 추천 채포, 운동 자세 조언, 학습 정리 도미처럼 본인이 다루기 쉬운 도메인을 고르면 됩니다. 도메인을 무엇으로 정하느냐에 따라 시스템 프롬프트의 역할과 데모 질문의 방향이 달라집니다. 프로젝트 완료 기준입니다. 첫째, 도메인이 분명한 채봇 API가 동작해야 합니다. 둘째, 슬래시 엔드포인트가 정상 응답을 돌려 줘야 합니다. 셋째, 슬래시/스트림 엔드포인트로 스트리밍 응답도 확인할 수 있어야 합니다. 넷째, 잘못된 입력이 들어왔을 때 적절한 오류 응답이 나가야 합니다. 다섯째, 데모 질문 다섯 개에 대해 의도한 방향의 답변이 나와야 합니다. 여섯째, 5일차에서 배운 레이어드 구조에 맞게 코드가 분리되어 있어야 합니다.이 체크리스트는 그대로 발표 자료의 뼈대가 될 수 있습니다. 각 항목을 코드나 실행 화면으로 보여주면 자연스럽게 프로젝트 발표 흐름이 만들어집니다. 프로젝트 진행 순서입니다. 먼저 도메인을 정합니다. 누구를 위한 채포인지 어떤 질문에 답하는 채포인지부터 정하는 것이 좋습니다. 그다음 도메인에 맞는 시스템 프롬프트 초안을 작성합니다. 역할 톤 언어 제약을 한두 문단 으로 정리해 봅니다. 이후 제공된 시작 코드를 실행하고 슬래시 엔드포인트가 정상 동작하는지 확인합니다. 그다음 데모 질문 다섯 개를 정해 실제 답변을 확인하고 답변의 결이 아쉬우면 시스템 프롬프트를 조금씩 다듬습니다. 마지막으로 슬래시체 슬/시스트림에서도 응답이 잘 흘러오는지 확인하고 발표 자료를 정리하면 됩니다.이 과정에서 가장 시간을 드릴 부분은 데모 질문을 정하고 시스템 프롬프트를 조정하는 단계입니다. 같은 모델이라도 시스템 프롬프트에 따라 답변의 결이 크게 달라지기 때문에 한 번에 끝내기보다 조금씩 수정하며 맞춰가는 것이 좋습니다. 이번 프로젝트에서 제공되는 것과 직접 채워야 하는 것을 구분해 보겠습니다. 제공되는 것은 5일차 레이어드 구조에 맞춰진 시작 코드 스켈레톤, 환경 설정 예시, 서버 실행 방법, 기본 동작 확인용 데모 입력입니다.이 부분은 그대로 따라 실행하면 기본 구조가 동작하도록 준비되어 있습니다. 여러분이 직접 채워야 하는 것은 도메인 인 시스템 프롬프트, 데모 질문 다섯 개, 그리고 도메인에 맞춘 간단한 응답 후처리입니다. 즉 코드에 큰 골격을 새로 만드는 것이 아니라 제공된 구조 위에 여러분의 도메인과 채봇의 성격을 입히는 작업이라고 보면 됩니다. 프로젝트 구조는 5일차 두 번째 섹션 실습에서 만든 구조를 그대로 이어갑니다. 앱 폴더 아래에 라우터스, 서비스, 클라이언트스, 스키마스, 코어 폴더가 있고 메인점파이에서 앱을 조립합니다.이 구조 로 위해서 서비스에는 시스템 프롬프트와 응답 처리 로직이 들어가고 스키마스에는 요청 데이터 구조가 들어갑니다. 라우터스는 HTTP 출입구를 담당하고 클라이언트스는 LLM 호출을 담당합니다. 골격은 그대로 두고 도메인에 맞는 내용만 채워 넣는 방식입니다. 시작 코드 실행 방법은 4일차와 5일차에서 사용한 방식과 비슷합니다. 먼저 답브 파일에 MLAPI 접속 정보를 채웁니다. 그다음 유비콘으로 앱.메인콜론앱을 실행합니다. 서버가 뜨면 슬래시독스에 접속해서 엔드포인트가 등록되어 있는지 확인합니다. 자세한 실행 방법은 프로젝트 가이드 노트북을 따라가면 됩니다. 여기서는 큰 흐름만 기억하면 됩니다. 엠블를 채우고 서버를 띄우고 슬래시독스에서 호출을 확인합니다. 도메인 선택에서 막힌다면 본인이 잘 아는 분야나 자주 사용하는 분야를 고르는 것이 좋습니다. 그래야 데모 질문을 만들기 쉽고 답변이 잘 나왔는 지도 판단하기 쉽습니다. 좋은 기준은이 채봇을 누가 쓸까라는 질문에 답할 수 있는지입니다. 예를 들어 다이어트 초보자를 위한 식단 상담 채포, 취업 준비생을 위한 자기 속에서 피드백 체포처럼 사용자가 분명하면 시스템 프롬프트도 훨씬 쓰기 쉬워집니다. 도메인은 거창할 필요가 없습니다. 작더라도 대상과 목적이 분명한 주제가 좋습니다. 도메인을 정했다면 시스템 프롬프트를 작성합니다. 3섹션에서 본 것처럼 역할 과 언어, 제약, 예시를 떠올리면 됩니다. 먼저 채봇이 어떤 역할로 답할지 정합니다. 그다음 친절하게 답할지, 짧고 명확하게 답할지, 한국어로만 답할지 같은 톤과 언어를 정합니다. 그리고 답변 길이, 피해야 할 내용, 반드시 포함할 내용 같은 제약을 넣습니다. 필요하다면 원하는 답변 예시를 한두 줄 넣어도 좋습니다. 시스템 프롬프트는 한 번에 완성하려고 하기보다 데모 질문을 돌려보고 답변을 보면서 조금씩 다듬는 것이 자연스럽습니다. 데모 질문 다섯 개도 미리 정해 두는 것이 좋습니다. 첫 번째는 도메인의 가장 전형적인 질문입니다. 채봇이 반드시 잘 답해야 하는 핵심 사례입니다. 두 번째는 도메인 안에 있지만 조금 까다로운 질문입니다. 시스템 프롬프트가 제대로 작동하는지 확인할 수 있습니다. 세 번째는 도메인의 경계에 걸친 질문입니다. 채봇이 어디까지 답하고 어디서 조심스럽게 안내하는지 보여 줄 수 있습니다.네 번째는 형식이 조금 다른 질문입니다. 짧게 답해 달라거나 목록으로 정리해 달라는 식의 요청을 넣어 볼 수 있습니다. 다섯 번째는 도메인과 무관한 질문입니다. 우리 채보시 주제밖에 요청해 어떻게 대응하는지 확인하는 용도입니다. 이렇게 다섯 개를 준비해 두면 발표에서도 흐름 있게 시현할 수 있습니다. 발표에 꼭 담으면 좋은 내용도 정리해 보겠습니다. 첫째, 어떤 도메인에 누구를 위한 채봇인지 설명합니다. 둘째, 시스템 프롬프트의 핵심 문장을 소개합니다. 셋째, 대모 질문 다섯 개와 실제 답변을 보여 줍니다. 넷째, 구현하면서 어려웠던 점과 어떻게 해결했는지를 이야기합니다. 다섯째, 더 다듬는다면 무엇을 개선할지 정리합니다. 발표는 길게 설명하기보다 도메인 선택 이후와 실제 동작 결과가 잘 보이도록 구성하는 것이 좋습니다. 짧게는 5분, 자세히는 10분 정도를 목표로 정리하면 됩니다. 마지막으로 프로젝트 발표입니다. 여러분이 만든 채포을 직접 시현하고 어떤 도메인을 선택했는지, 시스템 프롬프트를 어떻게 설계했는지, 실제 질문에 어떤 답변이 나오는지를 공유하는 시간입니다. 지난 5일 동안 우리는 API와 HTTP의 기초에서 시작해 패스트 API 서버를 만들고 LLM을 연동하고 스트리밍 응답을 구현했습니다. 그리고 마지막에는 실무형 구조, 프롬프트 관리, 로깅과 캐싱 같은 운영 요소까지 살펴보았습니다. 이번 미니 프로젝트는 그 내용을 하나로 묶어보는 마무리 활동입니다. 긴 시간 함께 해 주셔서 감사합니다. 모두 수고 많으셨습니다. 이것으로 패스트 API 기반 LLM 백엔드 API 개발과 스트리밍 실습 과정을 마치겠습니다.